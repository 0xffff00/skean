spring:

  output:
    ansi.enabled: DETECT

  jackson:
    serialization.write_dates_as_timestamps: false

  datasource:
    generate-unique-name: true

server:
  port: 18923

skean:
  jdbc:
    sql-builder:
      modify-all-rows-enabled: true

  id:

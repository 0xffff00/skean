spring:

  output:
    ansi.enabled: ALWAYS

  jackson:
    serialization.write_dates_as_timestamps: false

server:
  port: 18923

logging:
  level: DEBUG

skean:
  jdbc:
    sql-builder:
      modify-all-rows-enabled: true
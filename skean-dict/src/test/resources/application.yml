spring:

  output:
    ansi.enabled: DETECT

  jackson:
    serialization.write_dates_as_timestamps: false

  datasource:
    generate-unique-name: true

server:
  port: 18920

logging:
  level: DEBUG

skean:
  jdbc:
    sql-builder:
      modify-all-rows-enabled: true